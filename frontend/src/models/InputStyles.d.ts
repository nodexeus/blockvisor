type ButtonSize = 'small' | 'medium' | 'normal';

type ButtonStyle = 'primary' | 'secondary' | 'outline' | 'ghost';

type ButtonBorder = 'rounded';

type ButtonDisplay = 'inline' | 'block';

type InputSize = 'small' | 'medium' | 'large';

type InputStyle = 'default' | 'outline';

type UtilPosition = 'left' | 'right';

type AvatarSizes = 'small' | 'medium-small' | 'medium';

type IconButtonSize = 'tiny' | 'small' | 'medium';
